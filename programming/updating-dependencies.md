# Updating Dependencies

DO NOT `Update All` in the WPILib Vendor Deps tab all willy nilly. Updating the dependencies can result in your code no longer working. You should create a new branch dedicated to updating dependencies when necessary and test before merging into the base branch (`master` or `main`).
